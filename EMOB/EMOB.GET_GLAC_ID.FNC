CREATE OR REPLACE FUNCTION EMOB.GET_GLAC_ID (pGLCode VARCHAR2)
   RETURN NUMBER
IS
   vGlID   NUMBER;
BEGIN
   SELECT glac_id
     INTO vGlID
     FROM EMOB.MB_GLAC_MST
    WHERE cbs_gl = pGLCode;

   RETURN vGlID;
EXCEPTION
   WHEN OTHERS
   THEN
      RETURN NULL;
END;
/
